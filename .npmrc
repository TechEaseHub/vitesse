# 作用：强制将所有依赖项（包括 dependencies 和 devDependencies）提升到项目根目录的 node_modules 中。
# 默认行为（false）：pnpm 会使用“严格隔离”的方式安装依赖，每个包的依赖只存在于它自己的 node_modules 下。
# 启用后（true）：更接近 npm 或 yarn 的安装结构，兼容一些不正确依赖的旧库，尤其是没有声明 peerDependencies 或 hard-coded 寻找某些包路径的情况。
# ⚠️ 使用 shamefully-hoist=true 会牺牲 pnpm 的性能和空间优势，但提高兼容性。
shamefully-hoist=true
# 作用：禁用 peerDependencies 的严格检查。
# 默认行为（true）：安装过程中，如果某个依赖声明了未被满足的 peerDependencies，pnpm 会直接报错，安装失败。
# 设置为 false 后：即使某些 peerDependencies 不满足，pnpm 也会继续安装，发出警告但不中断。
# ⚠️ 某些包（尤其是 React 类库）对 peerDependencies 很依赖，这个设置可以避免因为未声明匹配版本而阻断安装，但可能导致运行时错误。
strict-peer-dependencies=false
